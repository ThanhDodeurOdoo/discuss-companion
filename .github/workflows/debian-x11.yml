name: Debian X11 (Trixie)

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    - cron: '0 0 * * *'

permissions:
  contents: read

jobs:
  verify:
    name: Debian X11 Verify
    runs-on: ubuntu-latest
    container:
      image: debian:trixie
    steps:
      - name: Install Debian system dependencies
        run: |
          apt-get update
          apt-get install -y --no-install-recommends \
            build-essential \
            ca-certificates \
            curl \
            git \
            libayatana-appindicator3-dev \
            libglib2.0-dev \
            libgtk-3-dev \
            libssl-dev \
            libsoup-3.0-dev \
            libwebkit2gtk-4.1-dev \
            librsvg2-dev \
            libx11-dev \
            libxtst-dev \
            pkg-config \
            python3 \
            xz-utils
          rm -rf /var/lib/apt/lists/*

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24.13.0"
          cache: "npm"
          cache-dependency-path: package-lock.json

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: 1.92.0
          components: clippy, rustfmt

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            app/backend/target
          key: debian-x11-cargo-verify-${{ hashFiles('app/backend/Cargo.lock') }}
          restore-keys: |
            debian-x11-cargo-verify-

      - name: Install dependencies
        run: npm install

      - name: Lint frontend & extension
        run: |
          npm run lint:extension
          npm run lint:frontend
          npm run typecheck

      - name: Lint backend (with X11)
        run: cd app/backend && cargo fmt --check && cargo clippy --features x11 --tests -- -D warnings

      - name: Test frontend & extension
        run: |
          npm run test:frontend
          npm run test:extension

      - name: Test backend (with X11)
        run: cd app/backend && cargo test --features x11
