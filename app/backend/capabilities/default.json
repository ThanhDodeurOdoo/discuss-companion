{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "call-controls"],
  "permissions": [
    "core:default",
    "opener:default",
    "positioner:default",
    "store:default"
  ]
}
